# Domain Model

Document Name: Domain Model
Version: 1.0
Source Documents: docs/prd.md
Generated By: AI Assistant
Date: 2026-06-19

---

# 1. Entity Relationship Overview

Berikut adalah entity-entity utama dalam sistem WebGIS Analisis Sebaran Toko dan Pesaing berdasarkan PRD:

- User
- Role
- Outlet
- Competitor
- LayerConfig
- AnalysisSession
- AnalysisResult
- ImportJob
- Report
- CoverageArea

---

# 2. Entity Detail

## 2.1 User

User yang terdaftar dalam sistem.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| UserID | UUID | Identifikasi unik user | PK, NOT NULL |
| Username | String | Nama pengguna untuk login | NOT NULL, UNIQUE |
| Email | String | Alamat email pengguna | NOT NULL, UNIQUE |
| NamaLengkap | String | Nama lengkap pengguna | NOT NULL |
| RoleID | UUID | Peran pengguna | FK, NOT NULL |
| StatusAktif | Boolean | Status akun aktif | DEFAULT: true |
| DibuatPada | Timestamp | Waktu pembuatan akun | NOT NULL |
| DiperbaruiPada | Timestamp | Waktu terakhir diperbarui | NOT NULL |

---

## 2.2 Role

Peran akses pengguna dalam sistem.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| RoleID | UUID | Identifikasi unik role | PK, NOT NULL |
| NamaRole | String | Nama peran (Admin, Manager, Analyst) | NOT NULL, UNIQUE |
| Deskripsi | String | Deskripsi peran | NULLABLE |
| CreatedAt | Timestamp | Waktu pembuatan role | NOT NULL |

### Kemampuan peran (sesuai PRD):

Admin
: Manajemen User, Outlet CRUD, Competitor CRUD, Analysis, Export

Manager
: Outlet CRUD, Competitor CRUD, Analysis, Export

Analyst
: Analysis, Export

---

## 2.3 Outlet

Outlet milik perusahaan yang dianalisis.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| OutletID | UUID | Identifikasi unik outlet | PK, NOT NULL |
| NamaOutlet | String | Nama outlet | NOT NULL |
| Alamat | String | Alamat lengkap outlet | NOT NULL |
| Latitude | Decimal | Koordinat latitude | NOT NULL |
| Longitude | Decimal | Koordinat longitude | NOT NULL |
| TipeOutlet | String | Tipe outlet (misal: Minimarket, Supermarket) | NOT NULL |
| Status | Enum | Status outlet (Aktif/Tutup/Renovasi) | NOT NULL |
| UserID | UUID | User yang menambahkan outlet | FK, NOT NULL |
| DibuatPada | Timestamp | Waktu pembuatan | NOT NULL |

---

## 2.4 Competitor

Kompetitor yang ada di pasaran.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| CompetitorID | UUID | Identifikasi unik kompetitor | PK, NOT NULL |
| NamaToko | String | Nama toko kompetitor | NOT NULL |
| Brand | String | Nama brand | NOT NULL |
| Kategori | String | Kategori kompetitor | NOT NULL |
| Latitude | Decimal | Koordinat latitude | NOT NULL |
| Longitude | Decimal | Koordinat longitude | NOT NULL |
| UserID | UUID | User yang menambahkan kompetitor | FK, NOT NULL |
| DibuatPada | Timestamp | Waktu pembuatan | NOT NULL |

---

## 2.5 LayerConfig

Konfigurasi layer peta yang dapat diaktifkan/nonaktifkan.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| LayerConfigID | UUID | Identifikasi unik layer config | PK, NOT NULL |
| UserID | UUID | Pemilik konfigurasi layer | FK, NOT NULL |
| OutletTampil | Boolean | Apakah layer outlet aktif | DEFAULT: true |
| KompetitorTampil | Boolean | Apakah layer kompetitor aktif | DEFAULT: true |
| AreaAdminTampil | Boolean | Apakah layer area administrasi aktif | DEFAULT: false |
| DiperbaruiPada | Timestamp | Waktu terakhir diperbarui | NOT NULL |

Available Layers (sesuai PRD):

- Outlet Sendiri
- Kompetitor
- Area Administrasi
- Kecamatan
- Kelurahan

---

## 2.6 AnalysisSession

Sesi analisis yang dilakukan oleh pengguna.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| AnalysisID | UUID | Identifikasi unik analisis | PK, NOT NULL |
| UserID | UUID | Pengguna yang melakukan analisis | FK, NOT NULL |
| JenisAnalisis | Enum | Jenis analisis (Radius, Heatmap, Coverage, WhiteSpot) | NOT NULL |
| Parameter | JSON | Parameter analisis (radius, layer, filter) | NOT NULL |
| Status | Enum | Status analisis (Proses, Selesai, Gagal) | DEFAULT: 'Proses' |
| MulaiPada | Timestamp | Waktu mulai analisis | NOT NULL |
| SelesaiPada | Timestamp | Waktu selesai analisis | NULLABLE |

---

## 2.7 AnalysisResult

Hasil dari proses analisis spasial.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| ResultID | UUID | Identifikasi unik hasil analisis | PK, NOT NULL |
| AnalysisID | UUID | Referensi ke sesi analisis | FK, NOT NULL |
| TipeHasil | Enum | Tipe hasil (Geojson, Statistik, Area) | NOT NULL |
| DataResult | JSON | Data hasil analisis dalam format GeoJSON atau array | NOT NULL |
| Ringkasan | String | Ringkasan hasil analisis dalam teks | NULLABLE |
| DibuatPada | Timestamp | Waktu pembuatan hasil | NOT NULL |

---

## 2.8 ImportJob

Job untuk import data massal (CSV/Excel).

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| ImportID | UUID | Identifikasi unik import | PK, NOT NULL |
| UserID | UUID | User yang melakukan import | FK, NOT NULL |
| TipeData | Enum | Tipe data (Outlet, Competitor) | NOT NULL |
| FilePath | String | Path file yang diupload | NOT NULL |
| Status | Enum | Status import (Menunggu, Proses, Selesai, Gagal) | DEFAULT: 'Menunggu' |
| TotalData | Integer | Jumlah total data dalam file | NOT NULL |
| ImportBerhasil | Integer | Jumlah data yang berhasil diimport | DEFAULT: 0 |
| ImportGagal | Integer | Jumlah data yang gagal diimport | DEFAULT: 0 |
| PesanError | String | Pesan error jika import gagal | NULLABLE |
| DibuatPada | Timestamp | Waktu pembuatan job import | NOT NULL |
| SelesaiPada | Timestamp | Waktu selesai import | NULLABLE |

---

## 2.9 Report

Laporan yang dihasilkan dari analisis.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| ReportID | UUID | Identifikasi unik laporan | PK, NOT NULL |
| AnalysisID | UUID | Referensi ke hasil analisis | FK, NOT NULL |
| UserID | UUID | User yang membuat laporan | FK, NOT NULL |
| FormatLaporan | Enum | Format laporan (PDF, Excel) | NOT NULL |
| FilePath | String | Path file laporan yang dihasilkan | NOT NULL |
| UkuranFile | Integer | Ukuran file laporan dalam bytes | NULLABLE |
| DibuatPada | Timestamp | Waktu pembuatan laporan | NOT NULL |

---

## 2.10 CoverageArea

Area cakupan yang dihitung dari hasil analisis.

| Attribute | Type | Description | Constraint |
| --------- | ---- | ----------- | ---------- |
| CoverageID | UUID | Identifikasi unik coverage area | PK, NOT NULL |
| ResultID | UUID | Referensi ke hasil analisis | FK, NOT NULL |
| AreaID | String | ID area administrasi (Kecamatan/Desa) | NOT NULL |
| NamaArea | String | Nama area | NOT NULL |
| LuasArea | Decimal | Luas area dalam km² | NULLABLE |
| PersentaseCoverage | Decimal | Persentase area yang terlayani | NOT NULL |
| JumlahOutlet | Integer | Jumlah outlet dalam area | DEFAULT: 0 |
| JumlahKompetitor | Integer | Jumlah kompetitor dalam area | DEFAULT: 0 |
| LabelArea | Enum | Kategori area (Terlayani, Tidak Terlayani) | NOT NULL |

---

# 3. Entity Relationship Diagram

```
User (1) ────< (N) Outlet
User (1) ────< (N) Competitor
User (1) ────< (N) AnalysisSession
User (1) ────< (N) ImportJob
User (1) ────< (N) Report
User (N) ──── (1) Role

AnalysisSession (1) ────< (1) AnalysisResult
AnalysisResult (1) ────< (N) CoverageArea
AnalysisResult (1) ────< (N) Report

ImportJob (N) ────< (N) Outlet
ImportJob (N) ────< (N) Competitor
```

---

# 4. Enum Definitions

### StatusOutlet

- `AKTIF`
- `TUTUP`
- `RENOVASI`

### JenisAnalisis

- `RADIUS`
- `HEATMAP`
- `COVERAGE`
- `WHITESPOT`
- `COMPETITOR`

### TipeHasil

- `GEOJSON`
- `STATISTIK`
- `AREA`

### TipeDataImport

- `OUTLET`
- `COMPETITOR`

### FormatLaporan

- `PDF`
- `EXCEL`

### LabelArea

- `TERLAYANI`
- `TIDAK_TERLAYANI`

### StatusAkun

- `AKTIF`
- `NONAKTIF`

---

# 5. Traceability

Domain Model ini berelasi dengan dokumen-dokumen berikut dalam chain turunan:

PRD
--> AI Planning Spec (format dokumen, aturan)
--> Domain Model (data structure)

Entitas pada Domain Model berasal dari requirement berikut pada PRD:

-FR-001: Authentication -> Entity: User, Role
-FR-002: User Management -> Entity: User, Role
-FR-003: Outlet Management -> Entity: Outlet, ImportJob
-FR-004: Competitor Management -> Entity: Competitor, ImportJob
-FR-005: GIS Map Visualization -> Entity: LayerConfig
-FR-006: Radius Analysis -> Entity: AnalysisSession, AnalysisResult
-FR-007: Coverage Analysis -> Entity: AnalysisSession, AnalysisResult, CoverageArea
-FR-008: Competitor Analysis -> Entity: AnalysisSession, AnalysisResult
-FR-009: Heatmap Analysis -> Entity: AnalysisSession, AnalysisResult
-FR-010: Potential Area Detection -> Entity: AnalysisSession, AnalysisResult, CoverageArea
-FR-011: Dashboard Analytics -> Entity: AnalysisResult
-FR-012: Reporting -> Entity: Report
