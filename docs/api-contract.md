# API Contract

Document Name: API Contract
Version: 1.0
Unique ID: API-001
Source Documents: docs/ai-planning-spec.md, docs/srs.md, docs/system-architecture.md, docs/erd.md, docs/ui-spec.md
Generated By: AI Assistant
Date: 2026-06-19

---

# 1. Overview

API Contract ini mendefinisikan spesifikasi lengkap untuk Backend API Golang + Go Fiber yang menjadi inti dari sistem WebGIS Analisis Sebaran Toko dan Pesaing. Dokumen ini mengacu pada:

- SRS Section 6.2 untuk endpoint API
- ERD untuk struktur data entities
- System Architecture untuk responsibilitas service layer
- UI Specification untuk behavior dan validasi input

---

# 2. Authentication API

## 2.1 POST /auth/login

**Description:** Autentikasi pengguna dan generate JWT token.

| Attribute | Value |
|-----------|-------|
| **API ID** | AUTH-001 |
| **Source Requirement** | SRS FR-001 |
| **Domain Entities** | User, Role |
| **Module** | MOD-001 Authentication |

### Request

```json
POST /auth/login
Content-Type: application/json

{
  "username": "string (required)",
  "password": "string (required)"
}
```

| Field | Type | Required | Validation | Source |
|-------|------|----------|------------|--------|
| username | string | Yes | Tidak boleh kosong | SRS 3.1.2 |
| password | string | Yes | Tidak boleh kosong | SRS 3.1.2 |

### Response

**Success (200):**
```json
{
  "access_token": "string",
  "token_type": "Bearer",
  "expires_in": "integer",
  "user": {
    "user_id": "uuid",
    "username": "string",
    "email": "string",
    "nama_lengkap": "string",
    "role": {
      "role_id": "uuid",
      "nama_role": "string"
    }
  }
}
```

**Error (401):**
```json
{
  "error": "Invalid credentials",
  "message": "Username atau password salah"
}
```

---

## 2.2 POST /auth/logout

**Description:** Logout pengguna dan invalidasi token.

| Attribute | Value |
|-----------|-------|
| **API ID** | AUTH-002 |
| **Source Requirement** | SRS FR-001 |
| **Module** | MOD-001 Authentication |

### Request

```json
POST /auth/logout
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "message": "Logout berhasil"
}
```

---

## 2.3 POST /auth/reset-password

**Description:** Request reset password via email.

| Attribute | Value |
|-----------|-------|
| **Source Requirement** | SRS FR-001 |
| **Module** | MOD-001 Authentication |

### Request

```json
POST /auth/reset-password
Content-Type: application/json

{
  "email": "string (required)"
}
```

### Response

**Success (200):**
```json
{
  "message": "Link reset password telah dikirim ke email"
}
```

---

# 3. User Management API

## 3.1 POST /users

**Description:** Membuat pengguna baru.

| Attribute | Value |
|-----------|-------|
| **API ID** | USER-001 |
| **Source Requirement** | SRS FR-002 |
| **Domain Entities** | User, Role |
| **Module** | MOD-002 User Management |
| **RBAC** | Admin only |

### Request

```json
POST /users
Authorization: Bearer <token>
Content-Type: application/json

{
  "username": "string (required)",
  "email": "string (required)",
  "nama_lengkap": "string (required)",
  "role_id": "uuid (required)",
  "password": "string (required)"
}
```

### Response

**Success (201):**
```json
{
  "user_id": "uuid",
  "username": "string",
  "email": "string",
  "nama_lengkap": "string",
  "role": {
    "role_id": "uuid",
    "nama_role": "string"
  },
  "status_aktif": true,
  "dibuat_pada": "timestamp"
}
```

---

## 3.2 GET /users

**Description:** Mendapatkan daftar pengguna.

| Attribute | Value |
|-----------|-------|
| **API ID** | USER-002 |
| **Source Requirement** | SRS FR-002 |
| **RBAC** | Admin only |

### Request

```json
GET /users
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "data": [
    {
      "user_id": "uuid",
      "username": "string",
      "email": "string",
      "nama_lengkap": "string",
      "role": {
        "role_id": "uuid",
        "nama_role": "string"
      },
      "status_aktif": true,
      "dibuat_pada": "timestamp"
    }
  ]
}
```

---

## 3.3 PUT /users/:id

**Description:** Update data pengguna.

| Attribute | Value |
|-----------|-------|
| **API ID** | USER-003 |
| **Source Requirement** | SRS FR-002 |
| **RBAC** | Admin only |

### Request

```json
PUT /users/:id
Authorization: Bearer <token>
Content-Type: application/json

{
  "username": "string (optional)",
  "email": "string (optional)",
  "nama_lengkap": "string (optional)",
  "role_id": "uuid (optional)",
  "status_aktif": "boolean (optional)"
}
```

### Response

**Success (200):**
```json
{
  "user_id": "uuid",
  "username": "string",
  "email": "string",
  "nama_lengkap": "string",
  "role": {
    "role_id": "uuid",
    "nama_role": "string"
  },
  "status_aktif": true,
  "diperbarui_pada": "timestamp"
}
```

---

## 3.4 DELETE /users/:id

**Description:** Hapus pengguna.

| Attribute | Value |
|-----------|-------|
| **API ID** | USER-004 |
| **Source Requirement** | SRS FR-002 |
| **RBAC** | Admin only |

### Request

```json
DELETE /users/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "message": "User berhasil dihapus"
}
```

---

# 4. Outlet Management API

## 4.1 POST /outlets

**Description:** Membuat outlet baru.

| Attribute | Value |
|-----------|-------|
| **API ID** | OUTLET-001 |
| **Source Requirement** | SRS FR-003 |
| **Domain Entities** | Outlet |
| **Module** | MOD-003 Outlet Management |
| **RBAC** | Admin, Manager |

### Request

```json
POST /outlets
Authorization: Bearer <token>
Content-Type: application/json

{
  "nama_outlet": "string (required)",
  "alamat": "string (required)",
  "latitude": "decimal (required)",
  "longitude": "decimal (required)",
  "tipe_outlet": "string (required)",
  "status": "enum AKTIF|TUTUP|RENOVASI (required)"
}
```

### Response

**Success (201):**
```json
{
  "outlet_id": "uuid",
  "nama_outlet": "string",
  "alamat": "string",
  "latitude": "decimal",
  "longitude": "decimal",
  "tipe_outlet": "string",
  "status": "string",
  "user_id": "uuid",
  "dibuat_pada": "timestamp"
}
```

---

## 4.2 GET /outlets

**Description:** Mendapatkan daftar outlet.

| Attribute | Value |
|-----------|-------|
| **API ID** | OUTLET-002 |
| **Source Requirement** | SRS FR-003 |
| **Module** | MOD-003 Outlet Management |

### Request

```json
GET /outlets
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "data": [
    {
      "outlet_id": "uuid",
      "nama_outlet": "string",
      "alamat": "string",
      "latitude": "decimal",
      "longitude": "decimal",
      "tipe_outlet": "string",
      "status": "string",
      "user_id": "uuid",
      "dibuat_pada": "timestamp"
    }
  ]
}
```

---

## 4.3 PUT /outlets/:id

**Description:** Update data outlet.

| Attribute | Value |
|-----------|-------|
| **API ID** | OUTLET-003 |
| **Source Requirement** | SRS FR-003 |
| **RBAC** | Admin, Manager |

### Request

```json
PUT /outlets/:id
Authorization: Bearer <token>
Content-Type: application/json

{
  "nama_outlet": "string (optional)",
  "alamat": "string (optional)",
  "latitude": "decimal (optional)",
  "longitude": "decimal (optional)",
  "tipe_outlet": "string (optional)",
  "status": "enum AKTIF|TUTUP|RENOVASI (optional)"
}
```

### Response

**Success (200):**
```json
{
  "outlet_id": "uuid",
  "nama_outlet": "string",
  "alamat": "string",
  "latitude": "decimal",
  "longitude": "decimal",
  "tipe_outlet": "string",
  "status": "string",
  "user_id": "uuid",
  "dibuat_pada": "timestamp"
}
```

---

## 4.4 DELETE /outlets/:id

**Description:** Hapus outlet.

| Attribute | Value |
|-----------|-------|
| **API ID** | OUTLET-004 |
| **Source Requirement** | SRS FR-003 |
| **RBAC** | Admin, Manager |

### Request

```json
DELETE /outlets/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "message": "Outlet berhasil dihapus"
}
```

---

## 4.5 POST /outlets/import

**Description:** Import data outlet via CSV/Excel.

| Attribute | Value |
|-----------|-------|
| **API ID** | OUTLET-005 |
| **Source Requirement** | SRS FR-003 |
| **Domain Entities** | Outlet, ImportJob |
| **Module** | MOD-009 Import Processing |
| **RBAC** | Admin, Manager |

### Request

```json
POST /outlets/import
Authorization: Bearer <token>
Content-Type: multipart/form-data

file: CSV/Excel file
```

### Response

**Success (200):**
```json
{
  "import_id": "uuid",
  "status": "PROSES",
  "total_data": "integer",
  "message": "Import dimulai"
}
```

---

# 5. Competitor Management API

## 5.1 POST /competitors

**Description:** Membuat data kompetitor baru.

| Attribute | Value |
|-----------|-------|
| **API ID** | COMP-001 |
| **Source Requirement** | SRS FR-004 |
| **Domain Entities** | Competitor |
| **Module** | MOD-004 Competitor Management |
| **RBAC** | Admin, Manager |

### Request

```json
POST /competitors
Authorization: Bearer <token>
Content-Type: application/json

{
  "nama_toko": "string (required)",
  "brand": "string (required)",
  "kategori": "string (required)",
  "latitude": "decimal (required)",
  "longitude": "decimal (required)"
}
```

### Response

**Success (201):**
```json
{
  "competitor_id": "uuid",
  "nama_toko": "string",
  "brand": "string",
  "kategori": "string",
  "latitude": "decimal",
  "longitude": "decimal",
  "user_id": "uuid",
  "dibuat_pada": "timestamp"
}
```

---

## 5.2 GET /competitors

**Description:** Mendapatkan daftar kompetitor.

| Attribute | Value |
|-----------|-------|
| **API ID** | COMP-002 |
| **Source Requirement** | SRS FR-004 |

### Request

```json
GET /competitors
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "data": [
    {
      "competitor_id": "uuid",
      "nama_toko": "string",
      "brand": "string",
      "kategori": "string",
      "latitude": "decimal",
      "longitude": "decimal",
      "user_id": "uuid",
      "dibuat_pada": "timestamp"
    }
  ]
}
```

---

## 5.3 PUT /competitors/:id

**Description:** Update data kompetitor.

| Attribute | Value |
|-----------|-------|
| **API ID** | COMP-003 |
| **Source Requirement** | SRS FR-004 |
| **RBAC** | Admin, Manager |

### Request

```json
PUT /competitors/:id
Authorization: Bearer <token>
Content-Type: application/json

{
  "nama_toko": "string (optional)",
  "brand": "string (optional)",
  "kategori": "string (optional)",
  "latitude": "decimal (optional)",
  "longitude": "decimal (optional)"
}
```

### Response

**Success (200):**
```json
{
  "competitor_id": "uuid",
  "nama_toko": "string",
  "brand": "string",
  "kategori": "string",
  "latitude": "decimal",
  "longitude": "decimal",
  "user_id": "uuid",
  "dibuat_pada": "timestamp"
}
```

---

## 5.4 DELETE /competitors/:id

**Description:** Hapus kompetitor.

| Attribute | Value |
|-----------|-------|
| **API ID** | COMP-004 |
| **Source Requirement** | SRS FR-004 |
| **RBAC** | Admin, Manager |

### Request

```json
DELETE /competitors/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "message": "Competitor berhasil dihapus"
}
```

---

# 6. Map API

## 6.1 GET /map/layers

**Description:** Mendapatkan konfigurasi layer peta.

| Attribute | Value |
|-----------|-------|
| **API ID** | MAP-001 |
| **Source Requirement** | SRS FR-005 |
| **Domain Entities** | LayerConfig |
| **Module** | MOD-005 GIS Map Visualization |

### Request

```json
GET /map/layers
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "layer_config_id": "uuid",
  "user_id": "uuid",
  "outlet_tampil": true,
  "kompetitor_tampil": true,
  "area_admin_tampil": false,
  "diperbarui_pada": "timestamp"
}
```

---

## 6.2 PUT /map/layers

**Description:** Update konfigurasi layer peta.

| Attribute | Value |
|-----------|-------|
| **API ID** | MAP-002 |
| **Source Requirement** | SRS FR-005 |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
PUT /map/layers
Authorization: Bearer <token>
Content-Type: application/json

{
  "outlet_tampil": "boolean (optional)",
  "kompetitor_tampil": "boolean (optional)",
  "area_admin_tampil": "boolean (optional)"
}
```

### Response

**Success (200):**
```json
{
  "layer_config_id": "uuid",
  "user_id": "uuid",
  "outlet_tampil": true,
  "kompetitor_tampil": true,
  "area_admin_tampil": false,
  "diperbarui_pada": "timestamp"
}
```

---

# 7. Analysis API

## 7.1 POST /analysis/radius

**Description:** Jalankan analisis radius jangkauan outlet.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-001 |
| **Source Requirement** | SRS FR-006 |
| **Domain Entities** | AnalysisSession, AnalysisResult |
| **Module** | MOD-006 Spatial Analysis |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
POST /analysis/radius
Authorization: Bearer <token>
Content-Type: application/json

{
  "outlet_id": "uuid (conditional)",
  "koordinat_pusat": {
    "latitude": "decimal (conditional)",
    "longitude": "decimal (conditional)"
  },
  "radius_meter": "integer (required)",
  "layer_data": ["string"]
}
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "RADIUS",
  "status": "PROSES",
  "parameter": {
    "outlet_id": "uuid",
    "radius_meter": "integer"
  },
  "mulai_pada": "timestamp"
}
```

---

## 7.2 GET /analysis/radius/:id

**Description:** Dapatkan hasil analisis radius.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-002 |
| **Source Requirement** | SRS FR-006 |

### Request

```json
GET /analysis/radius/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "RADIUS",
  "status": "SELESAI",
  "parameter": {},
  "mulai_pada": "timestamp",
  "selesai_pada": "timestamp",
  "results": [
    {
      "result_id": "uuid",
      "tipe_hasil": "GEOJSON|STATISTIK|AREA",
      "data_result": {},
      "ringkasan": "string"
    }
  ]
}
```

---

## 7.3 POST /analysis/coverage

**Description:** Jalankan analisis coverage wilayah.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-003 |
| **Source Requirement** | SRS FR-007 |
| **Domain Entities** | AnalysisSession, AnalysisResult, CoverageArea |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
POST /analysis/coverage
Authorization: Bearer <token>
Content-Type: application/json

{
  "outlet_ids": ["uuid"],
  "area_administrasi_ids": ["string"],
  "parameter_radius": "integer (required)"
}
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "COVERAGE",
  "status": "PROSES",
  "parameter": {
    "radius": "integer"
  },
  "mulai_pada": "timestamp"
}
```

---

## 7.4 GET /analysis/coverage/:id

**Description:** Dapatkan hasil analisis coverage.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-004 |
| **Source Requirement** | SRS FR-007 |

### Request

```json
GET /analysis/coverage/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "COVERAGE",
  "status": "SELESAI",
  "results": [
    {
      "result_id": "uuid",
      "tipe_hasil": "AREA",
      "coverage_areas": [
        {
          "coverage_id": "uuid",
          "area_id": "string",
          "nama_area": "string",
          "luas_area": "decimal",
          "persentase_coverage": "decimal",
          "jumlah_outlet": "integer",
          "jumlah_kompetitor": "integer",
          "label_area": "TERLAYANI|TIDAK_TERLAYANI"
        }
      ]
    }
  ]
}
```

---

## 7.5 POST /analysis/competitor

**Description:** Jalankan analisis kompetitor.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-005 |
| **Source Requirement** | SRS FR-008 |
| **Module** | MOD-006 Spatial Analysis |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
POST /analysis/competitor
Authorization: Bearer <token>
Content-Type: application/json

{
  "outlet_id": "uuid (required)",
  "radius_meter": "integer (required)"
}
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "COMPETITOR",
  "status": "PROSES",
  "parameter": {
    "outlet_id": "uuid",
    "radius_meter": "integer"
  },
  "mulai_pada": "timestamp"
}
```

---

## 7.6 GET /analysis/competitor/:id

**Description:** Dapatkan hasil analisis kompetitor.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-006 |
| **Source Requirement** | SRS FR-008 |

### Request

```json
GET /analysis/competitor/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "COMPETITOR",
  "status": "SELESAI",
  "results": [
    {
      "result_id": "uuid",
      "jarak_ke_kompetitor_terdekat": "decimal",
      "jumlah_kompetitor_dalam_radius": "integer",
      "ranking_area": []
    }
  ]
}
```

---

## 7.7 POST /analysis/heatmap

**Description:** Jalankan analisis heatmap.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-007 |
| **Source Requirement** | SRS FR-009 |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
POST /analysis/heatmap
Authorization: Bearer <token>
Content-Type: application/json

{
  "layer_data": "string (required)",
  "parameter_intensitas": "object (optional)"
}
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "HEATMAP",
  "status": "PROSES",
  "parameter": {
    "layer_data": "string",
    "intensitas": {}
  },
  "mulai_pada": "timestamp"
}
```

---

## 7.8 GET /analysis/heatmap/:id

**Description:** Dapatkan hasil analisis heatmap.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-008 |
| **Source Requirement** | SRS FR-009 |

### Request

```json
GET /analysis/heatmap/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "HEATMAP",
  "status": "SELESAI",
  "results": [
    {
      "result_id": "uuid",
      "tipe_hasil": "GEOJSON",
      "data_result": {}
    }
  ]
}
```

---

## 7.9 POST /analysis/whitespot

**Description:** Jalankan white spot analysis.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-009 |
| **Source Requirement** | SRS FR-010 |
| **Module** | MOD-006 Spatial Analysis |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
POST /analysis/whitespot
Authorization: Bearer <token>
Content-Type: application/json

{
  "outlet_ids": ["uuid"],
  "area_administrasi_ids": ["string"],
  "minimal_outlet": "integer (required)"
}
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "WHITESPOT",
  "status": "PROSES",
  "parameter": {
    "minimal_outlet": "integer"
  },
  "mulai_pada": "timestamp"
}
```

---

## 7.10 GET /analysis/whitespot/:id

**Description:** Dapatkan hasil white spot analysis.

| Attribute | Value |
|-----------|-------|
| **API ID** | ANALYSIS-010 |
| **Source Requirement** | SRS FR-010 |

### Request

```json
GET /analysis/whitespot/:id
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "analysis_id": "uuid",
  "jenis_analisis": "WHITESPOT",
  "status": "SELESAI",
  "results": [
    {
      "result_id": "uuid",
      "coverage_areas": [
        {
          "area_id": "string",
          "nama_area": "string",
          "skor_potensi": "integer",
          "label_area": "TIDAK_TERLAYANI"
        }
      ]
    }
  ]
}
```

---

# 8. Dashboard API

## 8.1 GET /dashboard/summary

**Description:** Mendapatkan ringkasan dashboard.

| Attribute | Value |
|-----------|-------|
| **API ID** | DASH-001 |
| **Source Requirement** | SRS FR-011 |
| **Module** | MOD-007 Dashboard Analytics |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
GET /dashboard/summary
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "total_outlet": "integer",
  "outlet_aktif": "integer",
  "total_kompetitor": "integer",
  "coverage_percentage": "decimal",
  "outlet_per_wilayah": []
}
```

---

## 8.2 GET /dashboard/outlet-stats

**Description:** Statistik outlet per wilayah.

| Attribute | Value |
|-----------|-------|
| **API ID** | DASH-002 |
| **Source Requirement** | SRS FR-011 |

### Request

```json
GET /dashboard/outlet-stats
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "data": [
    {
      "wilayah": "string",
      "jumlah_outlet": "integer",
      "jumlah_aktif": "integer"
    }
  ]
}
```

---

## 8.3 GET /dashboard/competitor-stats

**Description:** Statistik kompetitor per wilayah.

| Attribute | Value |
|-----------|-------|
| **API ID** | DASH-003 |
| **Source Requirement** | SRS FR-011 |

### Request

```json
GET /dashboard/competitor-stats
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
{
  "data": [
    {
      "wilayah": "string",
      "jumlah_kompetitor": "integer"
    }
  ]
}
```

---

# 9. Report API

## 9.1 POST /reports/generate

**Description:** Generate laporan PDF/Excel.

| Attribute | Value |
|-----------|-------|
| **API ID** | REPORT-001 |
| **Source Requirement** | SRS FR-012 |
| **Domain Entities** | Report, AnalysisSession |
| **Module** | MOD-008 Reporting |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
POST /reports/generate
Authorization: Bearer <token>
Content-Type: application/json

{
  "analysis_id": "uuid (required)",
  "format_laporan": "PDF|EXCEL (required)"
}
```

### Response

**Success (200):**
```json
{
  "report_id": "uuid",
  "analysis_id": "uuid",
  "format_laporan": "PDF|EXCEL",
  "file_path": "string",
  "status": "READY",
  "dibuat_pada": "timestamp"
}
```

---

## 9.2 GET /reports/:id/download

**Description:** Download file laporan.

| Attribute | Value |
|-----------|-------|
| **API ID** | REPORT-002 |
| **Source Requirement** | SRS FR-012 |
| **RBAC** | Admin, Manager, Analyst |

### Request

```json
GET /reports/:id/download
Authorization: Bearer <token>
```

### Response

**Success (200):**
```json
Binary file download (PDF/Excel)
```

**Headers:**
- `Content-Type`: `application/pdf` atau `application/vnd.openxmlformats-officedocument.spreadsheetml.sheet`
- `Content-Disposition`: `attachment; filename="report.pdf"`

---

# 10. Error Response Format

Semua endpoint mengembalikan format error yang konsisten:

```json
{
  "error": "string",
  "message": "string",
  "code": "string (optional)",
  "details": "object (optional)"
}
```

| HTTP Status | Kode Error | Keterangan |
|-------------|------------|------------|
| 400 | BAD_REQUEST | Input tidak valid |
| 401 | UNAUTHORIZED | Token tidak valid atau tidak ada |
| 403 | FORBIDDEN | Tidak memiliki akses |
| 404 | NOT_FOUND | Resource tidak ditemukan |
| 500 | INTERNAL_ERROR | Error server internal |

---

# 11. Authorization

Semua endpoint terprotected dengan JWT Bearer Token kecuali:
- `POST /auth/login`
- `POST /auth/reset-password`

**Header:**
```
Authorization: Bearer <access_token>
```

---

# 12. Enum References

| Enum | Nilai |
|------|-------|
| Status Outlet | `AKTIF`, `TUTUP`, `RENOVASI` |
| Jenis Analisis | `RADIUS`, `HEATMAP`, `COVERAGE`, `WHITESPOT`, `COMPETITOR` |
| Tipe Hasil | `GEOJSON`, `STATISTIK`, `AREA` |
| Status | `PROSES`, `SELESAI`, `GAGAL` |
| Format Laporan | `PDF`, `EXCEL` |
| Label Area | `TERLAYANI`, `TIDAK_TERLAYANI` |
| Tipe Import | `OUTLET`, `COMPETITOR` |
| Role | `Admin`, `Manager`, `Analyst` |

---

# 13. Traceability Matrix

| API ID | SRS Reference | ERD Entity | Module | RBAC |
|--------|---------------|------------|--------|------|
| AUTH-001 | FR-001 | User, Role | MOD-001 | Public |
| AUTH-002 | FR-001 | User, Role | MOD-001 | Authenticated |
| USER-001 | FR-002 | User, Role | MOD-002 | Admin |
| USER-002 | FR-002 | User, Role | MOD-002 | Admin |
| USER-003 | FR-002 | User, Role | MOD-002 | Admin |
| USER-004 | FR-002 | User, Role | MOD-002 | Admin |
| OUTLET-001 | FR-003 | Outlet | MOD-003 | Admin, Manager |
| OUTLET-002 | FR-003 | Outlet | MOD-003 | Admin, Manager |
| OUTLET-003 | FR-003 | Outlet | MOD-003 | Admin, Manager |
| OUTLET-004 | FR-003 | Outlet | MOD-003 | Admin, Manager |
| OUTLET-005 | FR-003 | Outlet, ImportJob | MOD-009 | Admin, Manager |
| COMP-001 | FR-004 | Competitor | MOD-004 | Admin, Manager |
| COMP-002 | FR-004 | Competitor | MOD-004 | Admin, Manager, Analyst |
| COMP-003 | FR-004 | Competitor | MOD-004 | Admin, Manager |
| COMP-004 | FR-004 | Competitor | MOD-004 | Admin, Manager |
| MAP-001 | FR-005 | LayerConfig | MOD-005 | Authenticated |
| MAP-002 | FR-005 | LayerConfig | MOD-005 | Authenticated |
| ANALYSIS-001 | FR-006 | AnalysisSession | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-002 | FR-006 | AnalysisResult | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-003 | FR-007 | AnalysisSession | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-004 | FR-007 | CoverageArea | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-005 | FR-008 | AnalysisSession | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-006 | FR-008 | AnalysisResult | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-007 | FR-009 | AnalysisSession | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-008 | FR-009 | AnalysisResult | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-009 | FR-010 | AnalysisSession | MOD-006 | Admin, Manager, Analyst |
| ANALYSIS-010 | FR-010 | CoverageArea | MOD-006 | Admin, Manager, Analyst |
| DASH-001 | FR-011 | AnalysisResult | MOD-007 | Admin, Manager, Analyst |
| DASH-002 | FR-011 | AnalysisResult | MOD-007 | Admin, Manager, Analyst |
| DASH-003 | FR-011 | AnalysisResult | MOD-007 | Admin, Manager, Analyst |
| REPORT-001 | FR-012 | Report | MOD-008 | Admin, Manager, Analyst |
| REPORT-002 | FR-012 | Report | MOD-008 | Admin, Manager, Analyst |

---

# 14. Document Control

## 14.1 Version History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 2026-06-19 | AI Assistant | Initial API Contract creation |

## 14.2 Source Declaration

Dokumen API Contract ini dihasilkan berdasarkan:
- `docs/ai-planning-spec.md` untuk format dokumen, unique ID, version, dan traceability
- `docs/srs.md` untuk definisi endpoint API dan requirement
- `docs/system-architecture.md` untuk service layer dan arsitektur backend
- `docs/erd.md` untuk entity schema PostgreSQL/PostGIS
- `docs/ui-spec.md` untuk behavior serta validasi input

Tidak ada endpoint, entity, atau field baru yang ditambahkan di luar source document.